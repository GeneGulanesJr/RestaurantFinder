# Avoid peer dependency resolution failures on CI (e.g. Cloudflare Pages).
# eslint-config-next plugins expect eslint ^9; we use eslint 10. Without this,
# npm can override and warn; some CI environments treat warnings or strict
# resolution as failure.
legacy-peer-deps=true
